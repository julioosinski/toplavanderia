[build]
  command = "pnpm build"
  publish = "dist"

# SPA fallback — arquivos estáticos em public/ (ex.: termos-de-uso.html) têm prioridade no Netlify
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
